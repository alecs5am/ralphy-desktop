{
  "name": "ralphy-desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Ralphy Desktop — embedded Claude Code chat over a Ralphy project.",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build:renderer": "vite build",
    "build:electron": "node scripts/build-electron.mjs",
    "start": "bun run build:renderer && bun run build:electron && electron .",
    "build": "bun run build:renderer && bun run build:electron",
    "preview": "vite preview"
  },
  "dependencies": {
    "framer-motion": "^12.39.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.0",
    "esbuild": "^0.24.2",
    "got": "^11.8.6",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
