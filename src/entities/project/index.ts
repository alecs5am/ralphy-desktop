/* The slice's public API: what the rest of the app may reach. Anything not re-exported here
   is internal to entities/project, and moving it is nobody else's business. */
export * from "./model/domain";
